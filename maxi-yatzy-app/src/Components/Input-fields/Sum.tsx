const Sum = () => (
    <input
        type="number"
        placeholder="0"
        className="player2-sum"
        max={126}
        min={0}
        style={{ width: "50px" }}
    />
);

export default Sum;
